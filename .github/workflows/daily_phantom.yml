# Daily Stock Data Update with Phantom Adjustment
# Runs Monday-Thursday at 4:05 PM EST
# Includes automatic RS adjustment to match IBD's 8,000 stock universe

name: Daily Phantom Adjustment

on:
  schedule:
    - cron: '5 21 * * 1-4'  # 4:05 PM EST = 21:05 UTC (Mon-Thu)
  workflow_dispatch:  # Allow manual trigger

jobs:
  daily-update-and-adjust:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Run daily update
      env:
        POLYGON_API_KEY: ${{ secrets.POLYGON_API_KEY }}
      run: |
        python process_stocks_daily.py
    
    - name: Adjust RS rankings with phantom stocks
      run: |
        echo "Adding phantom stocks to match IBD's 8,000 stock universe..."
        python adjust_phantom.py
        
        # Replace rankings.json with adjusted version
        cp rankings_adjusted.json rankings.json
        
        echo "✅ RS rankings adjusted successfully!"
    
    - name: Configure Git
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
    
    - name: Commit and push changes
      run: |
        git add rankings.json rankings_adjusted.json historical_data.json
        git diff --quiet && git diff --staged --quiet || (git commit -m "Daily update with phantom-adjusted RS rankings - $(date +'%Y-%m-%d %H:%M:%S')" && git push)
